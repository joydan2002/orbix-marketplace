# Nixpacks configuration for PHP 8.1
[variables]
PHP_VERSION = "8.1"

[phases.setup]
nixPkgs = ["php81", "php81Packages.composer"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader"]

[phases.start]
cmd = "php -S 0.0.0.0:$PORT -t public"
